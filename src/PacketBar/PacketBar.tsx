import * as React from 'react';

const PacketBar: React.FC = () => {

    return(
        <div style={{"backgroundColor": "powderBlue", "width": "25%"}}>
            PacketBar Component   
        </div>
    );
};

export default PacketBar;
